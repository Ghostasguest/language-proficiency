create a gui using python which contains a form having 8 items to fill
1: Serial number
2: Title - it contains an interactive textinput box only the but should be editable
3: Category - it contains an interactive Text inpput box but should be editable
4: Video upload button - this button should appear before uploading the video and after uploading it should display the latest path of the video
                         Working:- when the button is clicked a windows dialog box with file seelction should appear. when the reqiured file is selected and clicked on ok,
                         it should copy the file into a predefined app directory. 
5: Privacy status - this should contain 2 options (public or private) then user should have chance to select between these two options
6: Description - it contains an interactive textinput box instead of the text input box but should be editable
7: Key words - it contains an interactive textinput box instead of the text input box but should be editable
8: Date and time cell - it should contain a date and time selector. it should be interactive and usable for the user.

The user experience matters.
All the info should be updated in the excel sheet with the name " userdata.xlsx"


Errors encountered in the first round:
1. the video upload button is working but the file location is not being saved in the excel file instead the thumbnail png is getting saved thsi shouldnt happen
2. the date and time selector should be used to select date and time in the respective columns
3. there should be a save button for each row for saving the info manually. instead of saving automatically.
4. When the app is runned again, the app should display all the previously created rows as they are.
5. the rows size should be increased vertically make it less congested make the window size flexible. 
6. in the fourth row display the video path instead of the thumbnail
7. give the upload for each of the row to trigger a python script
8. the data is not getting saved 


now add these into

1st column:
tk.Label(root, text="Serial Number").pack(pady=5)
serial_number_entry = tk.Entry(root)
serial_number_entry.pack(pady=5)

2nd column:
tk.Label(root, text="Title").pack(pady=5)
title_entry = tk.Entry(root)
title_entry.pack(pady=5)

3rd column:
tk.Label(root, text="Category").pack(pady=5)
category_entry = tk.Entry(root)
category_entry.pack(pady=5)

4th column:
video_path = tk.StringVar()
tk.Label(root, text="Video Upload").pack(pady=5)
video_upload_button = tk.Button(root, text="Upload Video", command=upload_video)
video_upload_button.pack(pady=5)
video_path_label = tk.Label(root, textvariable=video_path)
video_path_label.pack(pady=5)

5th column:
tk.Label(root, text="Privacy Status").pack(pady=5)
privacy_status = tk.StringVar()
privacy_status.set("Public")
privacy_options = ttk.Combobox(root, textvariable=privacy_status, values=["Public", "Private"])
privacy_options.pack(pady=5)

6th column:
tk.Label(root, text="Description").pack(pady=5)
description_entry = tk.Entry(root)
description_entry.pack(pady=5)

7th column:
tk.Label(root, text="Keywords").pack(pady=5)
keywords_entry = tk.Entry(root)
keywords_entry.pack(pady=5)

8th column:
tk.Label(root, text="Date").pack(pady=5)
date_picker = DateEntry(root, date_pattern='yyyy-mm-dd')
date_picker.pack(pady=5)
tk.Label(root, text="Time").pack(pady=5)

9th column:
hour_selector = ttk.Combobox(root, values=[f"{i:02d}" for i in range(24)], width=2)
hour_selector.set(datetime.now().strftime("%H"))
hour_selector.pack(side=tk.LEFT, padx=(60, 2))
minute_selector = ttk.Combobox(root, values=[f"{i:02d}" for i in range(60)], width=2)
minute_selector.set(datetime.now().strftime("%M"))
minute_selector.pack(side=tk.LEFT)

This row with these items in each cell of the row should be created only when clicked on add row button